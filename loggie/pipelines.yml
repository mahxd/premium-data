pipelines:
  - name: test
    sources:
      - type: file
        name: mylog
        paths:
          - "/var/log/nginx/*.log"
    sink:
      type: elasticsearch
      hosts: ["es01:9200"]
      username: "elastic"
      password: "pas3@132"
      #caCertPath: /certs/ca/ca.crt
      index: "${+YYYY.MM.DD}"

